---
name: visualize-skills
description: 生成可独立打开或嵌入展示的交互式架构图、时序图和泳道图，用于解释组件依赖、同步调用和跨负责方的活动流转。不用于统计图表或界面设计。
---

# 生成交互图表

使用随 Skill 提供的 TypeScript 接口声明图表，生成包含样式与交互的 HTML 文件。每张图围绕一个问题；需要多个视角时分别生成。背景和结论写在正常回复里。执行生成需要能够运行 Node 和写文件的环境，查看图表需要现代浏览器。

## 选择与声明

按问题选择图表，读取对应参考和示例：

| 要表达的内容             | 参考                                 | 示例                                        |
| ------------------------ | ------------------------------------ | ------------------------------------------- |
| 组件依赖与分组           | [架构图](references/architecture.md) | [architecture.ts](examples/architecture.ts) |
| 同步调用、返回与条件分支 | [时序图](references/sequence.md)     | [sequence.ts](examples/sequence.ts)         |
| 活动在负责方之间的流转   | [泳道图](references/swimlane.md)     | [swimlane.ts](examples/swimlane.ts)         |

在任务工作目录编写 `diagram.ts`，从本 Skill 的 `src/index.ts` 导入接口，默认导出 `architecture(...)`、`sequence(...)` 或 `swimlane(...)`。对象、职责、关系样式和连接边的用法见[公共 API](references/api.md)。布局、绘制和交互由生成器提供。

名称和关系标签保持简短，关系应忠于实际含义。架构分区按共同职责或归属组织组件；泳道按负责方组织活动；时序调用必须配对返回。分区和泳道都不能作为关系端点。

节点尺寸、架构与泳道节点位置须显式声明。按主要流程对齐节点中心，并为关系标签留白；局部坐标与容量限制见对应图表参考。容器变窄时，图形和文字一起等比例缩小，因此内容过密时应按问题拆图。

## 生成与展示

需要 Node.js 22.18+，无需安装依赖或 build。将 `<skill>` 替换为本 Skill 的绝对路径：

```sh
node "<skill>/src/cli.ts" diagram.ts --format page -o output/diagram.html
```

`page` 表示独立页面，内嵌全部样式和交互，直接用浏览器打开即可使用，不需要服务器或联网。默认配色跟随浏览器的明暗偏好。给用户提供生成文件的链接；环境支持时打开预览。

输出到任务工作目录下的 `output/`，或用户指定的位置。内容脚本的相对导入以脚本位置为基准；命令的相对路径以当前工作目录为基准。

在支持原生可视化内容引用的 Codex 对话中展示时，读取 [Codex 展示](references/codex.md)，使用 `fragment`（嵌入片段）格式。片段继承宿主主题；通用浏览器交付使用 `page`。两种输出共用同一份图表和交互。

## 检查与调整

生成时自动检查语义、尺寸和布局。失败后按诊断修改声明再生成，不修改 HTML 绕过检查。`--check` 仅执行检查，不写文件。

确认生成文件完整，并检查实际展示中的文字可读性、连线和区域边界。悬停或键盘聚焦节点会强调直接相邻的节点和关系；悬停关系会强调其两个端点。检查窄容器中的阅读效果，必要时缩短标签或拆图。

文字或连线放不下时，按诊断调整节点尺寸、位置或留白。预期直连却出现折线时，检查节点中心是否对齐，以及标签和区域标题是否占用了连接空间。
