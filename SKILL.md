---
name: visualize-skills
description: 解释软件架构、项目如何运作或调用流程时，按问题选择或组合交互式架构图、时序图和泳道图，分别说明组件关系、调用过程与负责方之间的活动流转，无需用户明确要求画图。可嵌入对话或生成独立 HTML。不用于统计图表、界面设计或简单事实问答。
---

# visualize skill

使用随 Skill 提供的 TypeScript SDK 生成交互式图表。背景和结论写在正常回复里。

## 1. 选择图表

根据问题选择图表，需要多个视角时分别生成。

| 要表达的内容                   | 图表   | SDK 定义                  | 示例                     |
| ------------------------------ | ------ | ------------------------- | ------------------------ |
| 职责划分、组件依赖与系统边界   | 架构图 | src/architecture/index.ts | examples/architecture.ts |
| 调用链、请求生命周期与异步消息 | 时序图 | src/sequence/index.ts     | examples/sequence.ts     |
| 跨负责方的活动与流程流转       | 泳道图 | src/swimlane/index.ts     | examples/swimlane.ts     |

架构图先确定职责边界和抽象层次，再选择代表组件。分区表达共同职责、归属或部署边界，内部只保留解释边界所需的组件；跨区连线说明依赖的能力或契约。避免把类清单、方法调用或执行步骤逐一改画成节点；需要解释先后顺序时使用时序图或泳道图。没有有意义的边界时，不必添加分区。

画图之前先回答这张图让读者明白什么：一张图只回答一个问题；每个节点都应至少被一条有 `label` 的关系解释，解释不了的关系和为它存在的节点一起删掉；节点以 5 到 9 个为目标，超出时按职责或阶段拆图。三种图的分组字段方向一致但规则不同：架构分区的 `partition` 可声明 `position`；时序分区的 `partition` 要求成员在 `participants` 中连续排列，不接受 `position`；泳道的 `lane` 是每个节点的必填归属。

## 2. 阅读 SDK

从 `src/index.ts` 导入 SDK。阅读 `src/types.ts` 中的共用声明类型，以及所选图表的 `index.ts` 和示例，即可了解输入字段、约束与默认值。节点的 `role` 可以省略，省略时使用中性默认角色，不进入图例与调色板；需要按职责区分颜色时再声明。调用 `render(diagram)` 生成嵌入片段，或调用 `renderPage(diagram)` 生成独立页面；两者都会检查声明与布局，失败时抛出 `DiagnosticError`。调用 `inspect(diagram)` 获取结构化几何报告：先是布局问题警告 `warnings`（近乎对齐、可消除的折弯、标签拥挤），再是画布、节点矩形与中心、容器、连线路径和标签位置。修正布局时用它读取坐标，不要解析渲染出的 HTML。

## 3. 生成与修正

编写内容脚本，直接用 Node 执行并保存 HTML。主动利用 SDK 的布局语义表达阅读顺序、系统边界和主次关系。布局意图明确时，初稿即可声明位置与连接边；没有明确意图的部分交给自动布局。SDK 校验通过只表示满足程序检查的约束；每张图仍须完成下一节的图像复核，才能交付。

- **布局方向**：根据关系和展示宽度选择横向、纵向或混合布局。并列职责适合横向比较，真实的层级依赖适合上下排列；不要仅因箭头有方向就排成长链。架构图用 `direction` 选择自动分层方向（默认 `vertical` 自上而下，`horizontal` 自左向右），分区内部和根级别都遵循；混合组织方式用 `position` 表达。检查整图宽高与留白，避免横向空置而纵向不断延伸，也不要为横排缩小文字。
- **分组与位置**：架构节点用可选的 `partition` 字段声明归属分区，与时序参与者的 `partition`、泳道节点的 `lane` 方向一致。需要两个节点中心对齐时，优先声明 `align: { with, axis }`：`axis: "x"` 对齐水平中心 cx 用于垂直直连，`"y"` 对齐垂直中心 cy 用于水平直连，坐标由布局推导，无须计算；目标须在同一分区、同一泳道或同在未分组区域，且不能与 `position` 同时声明，用法见 examples/architecture-alignment.ts。用分区的 `position` 安排系统边界，用节点的 `position` 分开分支并为回流连线留白。分区与未分组节点的坐标相对画布内容原点，分区成员相对分区标题和内边距之后的内容原点；泳道节点相对泳道内容原点。尺寸由内容自动计算，不要把画布绝对坐标直接填入局部 `position`。
- **关系声明**：阅读 `src/types.ts` 中的 `RelationInput`，按真实方向声明 `from`、`to`，用简短的 `label` 说明连接含义，用 `variant` 区分需要强调的关系。主动考虑 `fromSide`、`toSide`，让主链路方向一致，让分支和回流从合适的边进出；这些字段是硬约束，无法寻路时需要调整。时序消息使用 `MessageInput`，通过参与者顺序与消息顺序组织布局，不支持这些连接边字段。
- **标签空间**：预览时检查文字是否挤在拐点、分区边界或其他连线附近，以及标签与对应连线是否容易辨认。通过调整节点或分区的 `position`、关系连接边和标签措辞留出空间，再重新生成并预览。
- **诊断修正**：有诊断时按 `DiagnosticError.diagnostics` 的位置和建议修改声明，再生成。保留真实关系，内容过密时按解释重点拆图。

## 4. 布局警告与图像复核

生成成功后，先运行 `inspect(diagram)` 并清零报告中的 `warnings`：每条警告给出位置、数值与修正建议，按提示调整 `position`、连接边或标签措辞后重新生成，直到不再出现警告。为表达连接边意图而声明的非对向边不会触发报警。

警告清零后，仍必须在浏览器中按实际展示宽度渲染 HTML，获取并查看截图，作为最终确认。只读 HTML/SVG、检查坐标、打开页面但不查看图像、收到零诊断或警告为零，都不能代替图像复核。

- **逐条检查连线**：沿每条连线从起点看到终点。对于预期水平或垂直直连、且中间没有障碍的关系，检查是否出现短折段、阶梯或不必要的绕行。中心点错位导致的折弯也必须修正，不能因为没有重叠或诊断就接受。分支、回流和避障所需的转折可以保留。
- **按实际中心对齐**：水平直连对齐两端节点的中心 `cy`，垂直直连对齐中心 `cx`，并选择相对的连接边。同一容器内直接为其中一端声明 `align: { with, axis }`，布局会把它的中心对齐到目标节点，无须读取坐标；警告的提示会给出具体声明。跨容器或需要偏移量时才用 `position`：用 `inspect(diagram)` 的报告计算，每个节点给出矩形、中心、所在容器、局部原点 `origin` 和已声明的 `position`；修正量换算回局部坐标即 `期望的绝对坐标 - origin`，已声明位置时叠加在原 `position` 上。节点左上角坐标相同不代表中心对齐，文字换行、描述和标签可能使节点尺寸不同。
- **检查整图**：确认主链路易读、连线没有不必要的交叉、标签归属清晰且不挤在拐点或边界附近、实际展示宽度下文字可读。

发现问题后修改源声明，重新生成、渲染、截图并查看；每次修改都要复查受影响的连线和整图。完成条件是最终版本同时通过 SDK 校验、警告清零与图像复核，最后一次修改之前的截图不能作为交付依据。若无法获取或查看渲染图像，应明确说明尚未完成图像复核，不能宣称视觉检查通过。

## 5. 交付

- **Codex 对话内展示**：读取并遵循 references/codex.md，生成嵌入片段，在本轮最终回复中完成展示。
- **独立浏览器页面或文件交付**：生成独立 HTML 页面并提供文件链接。用户明确要求独立文件时使用此方式。
