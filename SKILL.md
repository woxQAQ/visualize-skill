---
name: visualize-skills
description: 在 Codex 对话中绘制可交互的架构图、时序图和泳道图。用于展示组件依赖、同步调用或跨负责方的活动流转，支持节点详情和关联强调。不生成报告，不处理 Markdown 正文，不用于统计图表或界面设计。
---

# 在对话中画图

使用随 Skill 提供的 TypeScript 接口声明图表，生成可供 Codex 原生可视化容器直接展示的 HTML 片段。一次生成一张图；需要多个视角时分别生成，在对话中逐张展示。背景和结论写在正常回复里。

## 选择与声明

结构问题用架构图，同步调用过程用时序图，跨负责方的活动流转用泳道图。每张图围绕一个问题，名称和关系标签保持简短。对象表示实际组件或活动，不把执行者与其执行的活动当作同一对象。

按需查阅 [公共 API](references/api.md)、[架构图](references/architecture.md)、[时序图](references/sequence.md)或[泳道图](references/swimlane.md)。完整声明示例位于 `examples/architecture.ts`、`examples/sequence.ts` 和 `examples/swimlane.ts`。

在任务工作目录编写 `diagram.ts`，从本 Skill 的 `src/index.ts` 导入接口，默认导出 `architecture(...)`、`sequence(...)` 或 `swimlane(...)`。不创建文档或追加正文。接口、语义检查、布局、SVG 和节点交互均由本 Skill 提供，无需另写绘图代码或手工转换输出。

节点尺寸、架构与泳道节点位置由脚本声明，作为宽版排布。容器变窄时，生成器按阅读顺序重排节点、调整节点宽高并重新布线，保持文字字号。架构分区按共同职责或归属分组，不是关系端点；泳道按活动的负责方组织；时序调用必须配对返回。

先确定主要流程的行列中心线，再计算节点位置。上下直连时对齐水平中心，左右直连时对齐垂直中心；跨分区或泳道时用画布坐标比较。必要的分支、回退和避障转折可以保留，不为连通图形虚构关系。

## 生成与展示

需要 Node.js 22.18+，无需安装依赖或 build。将 `<skill>` 替换为本 Skill 的绝对路径：

```sh
node "<skill>/src/cli.ts" diagram.ts -o "<output>/diagram.html"
```

输出路径优先使用当前任务明确提供的可写 visualization 目录，否则使用任务工作目录下的 `output/`。不要猜测目录权限或写入系统临时目录。内容脚本的相对导入以脚本位置为基准；命令的相对路径以当前工作目录为基准。

生成器输出的就是 HTML 片段，已包含 SVG、样式和交互。颜色引用 Codex 提供的主题 token，即具有固定用途的 CSS 变量，随应用配色和明暗模式自动更新。不写死颜色、不锁定 `color-scheme`，不包裹完整 HTML 页面。

在同一轮最终回复中，用绝对路径输出以下内容引用，独占一行：

```text
visualize{"path":"<absolute-path>/diagram.html"}
```

这会将结果直接嵌入 Codex 对话。默认不提供 HTML 下载链接或打开外部浏览器。需要说明时，在图外用简短文字解释事实与结论。多个图各用一个内容引用，不合并成报告。

## 检查与调整

生成时自动检查语义、尺寸和布局。失败后按诊断修改声明再生成，不修改 HTML 绕过检查。`--check` 仅执行检查，不写文件。

读取生成片段，确认产物完整。检查显示效果时关注文字、连线、分区与泳道边界、调用顺序，以及悬停强调和键盘聚焦。节点详情目前停用，实现保留。图表高度随内容展开，不限制区域高度，不引入内部滚动。至少检查 320、620 和 1024 像素容器：图表应自动换用能放下的布局，不出现横向滚动条。极窄时序图按调用顺序逐条展开，保留条件分支和返回配对；复杂关系标签放不下时使用编号及关系图例。优先按问题拆图，避免一张图承载过多关系。

若预期直连的节点出现折线，依次检查画布中心线、连接间隙中的标签空间、分区标题障碍，再按原因修改声明。跨分区的留白规则见[架构图坐标说明](references/architecture.md#坐标与尺寸)。
