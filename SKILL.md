---
name: visualize-skills
description: 解释软件架构、项目如何运作或调用流程时，按问题选择或组合交互式架构图、时序图和泳道图，分别说明组件关系、调用过程与负责方之间的活动流转，无需用户明确要求画图。可嵌入对话或生成独立 HTML。不用于统计图表、界面设计或简单事实问答。
---

# visualize skill

使用随 Skill 提供的 TypeScript SDK 生成交互式图表。背景和结论写在正常回复里。

## 1. 选择图表

根据问题选择图表，需要多个视角时分别生成。

| 要表达的内容                   | 图表   | SDK 定义                  | 示例                     |
| ------------------------------ | ------ | ------------------------- | ------------------------ |
| 组件、服务与系统边界           | 架构图 | src/architecture/index.ts | examples/architecture.ts |
| 调用链、请求生命周期与异步消息 | 时序图 | src/sequence/index.ts     | examples/sequence.ts     |
| 跨负责方的活动与流程流转       | 泳道图 | src/swimlane/index.ts     | examples/swimlane.ts     |

## 2. 阅读 SDK

从 src/index.ts 和所选图表的定义、示例了解用法。声明、渲染和诊断的细节按需沿源码读取，以当前实现为准。

## 3. 生成与修正

编写内容脚本，直接用 Node 执行并保存 HTML。主动利用 SDK 的布局语义表达阅读顺序、系统边界和主次关系。布局意图明确时，初稿即可声明位置与连接边；没有明确意图的部分交给自动布局。生成成功不代表效果合适，应结合几何结果和实际预览修正。

- **分组与位置**：架构图在 `partition.nodes` 中集中声明成员实体或 ID，节点定义仍放在图表 `nodes` 中。用分区的 `position` 安排系统边界，用节点的 `position` 对齐主链路、分开分支并为回流连线留白。分区与未分组节点的坐标相对画布内容原点，分区成员相对分区标题和内边距之后的内容原点；泳道节点相对泳道内容原点。尺寸由内容自动计算，不要把画布绝对坐标直接填入局部 `position`。
- **关系声明**：阅读 `src/shared/model.ts` 中的 `RelationInput`，按真实方向声明 `from`、`to`，用简短的 `label` 说明连接含义，用 `variant` 区分需要强调的关系。主动考虑 `fromSide`、`toSide`，让主链路方向一致，让分支和回流从合适的边进出；这些字段是硬约束，无法寻路时需要调整。时序消息使用 `MessageInput`，通过参与者顺序与消息顺序组织布局，不支持这些连接边字段。
- **标签空间**：用 `compile(diagram)` 的 `scene` 检查节点矩形、关系 `points` 和标签位置。`labelX`、`labelY` 是标签文本块左上角的画布绝对坐标，结合 `label.width`、`label.height` 判断文字是否挤在拐点、分区边界或其他连线附近。它们是计算输出，不是 `RelationInput` 参数；通过调整声明中的位置、连接边和标签措辞改善布局，再重新编译，不直接修改输出几何。
- **预览与修正**：在实际展示宽度下检查主链路是否易读、连线是否绕远或交叉、标签归属是否清晰、整体缩放后文字是否过小。即使没有诊断，也要修正明显影响阅读的布局；有诊断时按 `DiagnosticError.diagnostics` 的位置和建议修改声明，再生成并复查。保留真实关系，内容过密时按解释重点拆图。

## 4. 交付

- **Codex 对话内展示**：读取并遵循 references/codex.md，生成嵌入片段，在本轮最终回复中完成展示。
- **独立浏览器页面或文件交付**：生成独立 HTML 页面并提供文件链接。用户明确要求独立文件时使用此方式。
